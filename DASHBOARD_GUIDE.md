# M7 Bot Dashboard - 사용 가이드

## 🚀 빠른 시작

### 1. 대시보드 실행

```bash
# Development 폴더로 이동
cd c:\Users\user\Desktop\Developement

# Streamlit 대시보드 실행
streamlit run dashboard.py
```

실행하면 자동으로 브라우저가 열리며 대시보드가 표시됩니다.
기본 URL: `http://localhost:8501`

---

## 📊 대시보드 기능

### 요약 지표 (상단)
- **총 신호 수**: 전체 저장된 신호 개수
- **강력 매수 신호**: STRONG BUY 신호 개수 및 비율
- **분석 종목 수**: 분석된 M7 종목 수
- **최근 신호**: 마지막 신호 생성 시간

### 필터 통과율
5개 필터의 통과율을 한눈에 확인:
- 거시경제 (Market)
- 차트 (Chart)
- 뉴스 (News)
- 옵션 (Options)
- 지지선 (Support)

### 최근 포착 종목
최근 분석된 종목을 버튼 형태로 표시 (클릭 가능)

### 신호 내역 테이블
- 생성일시, 종목, 신호, 진입가 표시
- 필터링 가능 (강력 매수, 관망 등)
- CSV 다운로드 지원

### 상세 필터 정보
- 개별 신호 선택 시 5개 필터 결과 상세 표시
- 각 필터의 통과/실패 여부 확인

---

## ⚙️ 사이드바 기능

### 데이터 새로고침
- 🔄 버튼 클릭으로 최신 데이터 로드
- 캐시 자동 갱신 (5분마다)

### 필터 옵션
- 표시할 신호 개수 조절 (10~200개)
- 신호 유형 필터 (강력 매수, 관망 등)

---

## 🔧 설정

### 환경 변수 (.env 파일)
```
SUPABASE_URL=your_supabase_url
SUPABASE_KEY=your_supabase_key
```

### Streamlit Secrets (배포 시)
`.streamlit/secrets.toml` 파일 생성:
```toml
SUPABASE_URL = "your_supabase_url"
SUPABASE_KEY = "your_supabase_key"
```

---

## 📱 배포 옵션

### Streamlit Cloud (무료)
1. GitHub에 코드 푸시
2. [share.streamlit.io](https://share.streamlit.io) 접속
3. 레포지토리 연결
4. Secrets 설정 (SUPABASE_URL, SUPABASE_KEY)
5. Deploy 클릭

### Heroku
```bash
# Procfile 생성
echo "web: streamlit run dashboard.py --server.port=$PORT" > Procfile

# 배포
heroku create your-app-name
git push heroku main
```

### Docker
```dockerfile
FROM python:3.9-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
EXPOSE 8501
CMD ["streamlit", "run", "dashboard.py"]
```

---

## 🐛 문제 해결

### 데이터가 표시되지 않을 때
1. `.env` 파일 확인
2. Supabase 연결 테스트: `python verify_data.py`
3. 인터넷 연결 확인

### 대시보드가 실행되지 않을 때
```bash
# Streamlit 재설치
pip install --upgrade streamlit

# 캐시 삭제
streamlit cache clear
```

### 포트 충돌 시
```bash
# 다른 포트로 실행
streamlit run dashboard.py --server.port 8502
```

---

## 💡 팁

### 자동 새로고침
대시보드는 5분마다 자동으로 캐시를 갱신합니다.
수동 새로고침: 사이드바의 🔄 버튼 클릭

### CSV 다운로드
신호 내역 테이블 하단의 "📥 CSV 다운로드" 버튼으로 데이터 내보내기 가능

### 모바일 접근
로컬 네트워크에서 다른 기기로 접근:
```
http://your-local-ip:8501
```

---

## 📞 지원

문제 발생 시:
1. `verify_data.py` 실행하여 DB 연결 확인
2. Streamlit 로그 확인
3. 브라우저 콘솔 확인

---

**버전**: Dashboard v1.0  
**최종 업데이트**: 2025-11-20
